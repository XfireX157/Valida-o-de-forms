export interface IColaborador {
    nome: string
    cargo: string
    image: string
    time?: string
    data: string
}

export interface INewColaborador {
    id?: number
    nome: string
    cor: string
}